// A photo currently on display — one of a destination/vibe's curated
// static fallback options.
export interface ResolvedImage {
  url: string
  photographerName?: string
  photographerUrl?: string
}
